/**
 * List of tags to use for validation in 'scripts/validate-frontmatter' build step.
 */

const pageTags = [`article`, `case-studies`, `contact`, `home`, `services`, `site`]

module.exports = [`code`, `Joomla!`, ...pageTags]
