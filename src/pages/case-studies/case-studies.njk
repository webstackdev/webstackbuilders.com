---
title: 'Case Studies'
description: 'Past work and projects of Webstack Builders'
tags: nav
permalink: /case-studies/
layout: base
eleventyNavigation:
  key: case-studies
  title: 'Case Studies'
  order: 2
---
<div class="page page--blog">
  <header class="page__header">
    <h1 class="page__title">{{ title }}</h1>
  </header>

  <div class="page__content">
    {%- set casestudieslist = collections.casestudies -%}
    <ol reversed class="articlelist" start="{{ casestudieslist.length }}">
      {% for casestudy in casestudieslist | reverse %}
        <li class="articlelist__item h-entry {% if casestudy.data.draft %}articlelist__item--draft{% endif %}">
          <h2 class="articlelist__title p-name">
            <a
              href="{{ casestudy.url | url }}"
              class="articlelist__link u-url"
              rel="bookmark"
            >
              {{ casestudy.data.title }}
            </a>
          </h2>

          <div class="articlelist__meta">
            <time class="articlelist__date dt-published" datetime="{{ casestudy.date | dateToISO }}">
              {{ casestudy.date | readableDate('dd LLL yyyy') }}
            </time>

            {% if casestudy.data.tags %}
              <span aria-hidden="true">&sdot;</span>
              <span class="articlelist__tags p-category">
                {{ casestudy.data.tags|join(', ') }}
              </span>
            {% endif %}
          </div>

          <p class="articlelist__excerpt p-summary">
            {% if casestudy.data.description %}
              {{ casestudy.data.description }}
            {% else %}
              {{ casestudy.templateContent | excerpt | striptags }}
            {% endif %}
          </p>
        </li>
      {% endfor %}
    </ol>
  </div>
</div>
