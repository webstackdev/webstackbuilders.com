---
layout: base
---
{#- Used to provide <article> element with header and content areas for pages that do not -#}
{#- extend 'base.njk' directly, generally simpler or Markdown templates for single pages -#}
{#- instead of major content areas. Used as 'layout' for '404.njk', 'offline.jk', -#}
{#- 'privacy.md', 'cookies.md', and 'index.njk' for 'about' and 'contact' pages -#}
<article class="page">
  <header class="page__header">
    <h1 class="page__title">{{ title }}</h1>
    {% if subtitle %}
      <p class="page__subtitle">{{ subtitle | safe }}</p>
    {% endif %}
  </header>
  <div class="page__content markdown">
    {{ content | safe }}
  </div>
</article>